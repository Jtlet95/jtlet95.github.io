// 1. ASSIGNMENT OPERATOR (=)
// the asignment operator "=" assigns a value to whatever is on the left 
// of the operator sign 
// EX: var x = 394 => x now has a value of 394 because we assigned it with the 
//Assignment operator 

// 2. ARITHMETIC OPERATORS(+,-,*,\)
//Arithmetic operators are operators we can you use to do mathmatic equations
//within our javascript terminal they can be used with number data and return 
// the data to the console 
//EX: 2+2 or var a = 1 , a + 2
//EX: 3-3 or var a = 3, b - 3
//EX: 4*4 or var c = 4, c * 4 
//EX: 4/2 or var c = 4  c/2 

//3. COMPARISION OPERATORS
//The Comparision operatiors are used to do operation comparisions to return
// a boolean value of true or false 
//   ==   equal to              1 == 1       true
//   ===  strict equal         2 === "2"     false
//   !=  not equal to(bang)     1 != 2       true
//   >    greater than          2 > 1        true
//   <     less than            5 < 6        true 
//   >=  greater than or equal  1 >= 1       true 
//   <=  less thaan or equal to  2 <= 1      false  

//LOGICAL OPERATORS (&&, ||, !)
// there are three logical operators  and (&&), or (||), bang (!)
// these operators can be applied to any types and be used as logical comparisions
// EX if(the sum is 2 || "2" && not !3){
    // return true  }
    
//UNARY  OPERATORS (.typeof(), ++, --)
//Unary operators are operators that take a single argument and perform 
// an operation like increment , decrement and typeof() 
// Ex: var x = [];
// Ex: (var i = 0; i > 10; i++){}, (var i = 20; i <10; i--){}, .typeof(x) => returns array

//TERNARY oPERATORS(if, ?)
//the ternary operator is a conditional statement that has diffrent out 
//put based on the condition that is true in the statement
// EX:  if ( condition ) {
//   value if true;
//   } else {
//   value if false;
// }
// EX: condition ? value if true : value if false