// 1. STRING MANIPULATION using (+)
// string manipulation using operators, before when i talked about operators 
// i refered to numbers however there are times when you will want to combine
//or concatanate two or more strings together and to do that you 
//have to you the "+"" (concatanate) operator
// EX: var newText1 = document.createTextNode('string1='+string1);
// EX: let x = "stringA" + " " + stringB + " " + stringsC " " = new result

// 2, STRING MANIPULATION using methods ()
// in javascript methods are like keywords that have purpose or function for 
// to use in our code, because strings are primitive, the langauge developers
// have put in some methods or properties that we can use to manipulate or see 
// things about our data
//EX: var x = "WOW"  
// EX: x.length => will return the length to the console 

// EX: splice() var str = "Apple, Banana, Kiwi";
// EX:     var res = str.slice(0 ,2);
// EX:   PRINTS : Banana 

// EX .toUpperCase and .toLowerCase catually change the case of the letters 
// within the string 